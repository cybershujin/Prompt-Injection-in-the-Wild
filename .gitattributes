# Normalize all text to LF in the repo so the Linux GitHub Action regenerates
# byte-identical files (no CRLF-vs-LF churn in the generated artifacts).
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
